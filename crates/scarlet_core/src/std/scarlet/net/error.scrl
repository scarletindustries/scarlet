// A network error. Most variants are an OS errno, named as in Rust's
// `std::io::ErrorKind`. `Errno` carries any errno without its own variant.
pub type NetError {
	// ETIMEDOUT, or a deadline passed.
	TimedOut
	// ECONNREFUSED
	ConnectionRefused
	// ECONNRESET
	ConnectionReset
	// ECONNABORTED
	ConnectionAborted
	// ENOTCONN
	NotConnected
	// EPIPE
	BrokenPipe
	// EADDRINUSE
	AddrInUse
	// EADDRNOTAVAIL
	AddrNotAvailable
	// ENETDOWN
	NetworkDown
	// ENETUNREACH
	NetworkUnreachable
	// EHOSTUNREACH
	HostUnreachable
	// EACCES, such as binding a privileged port.
	PermissionDenied
	// The peer closed before enough bytes arrived.
	UnexpectedEof
	// A message was bigger than the reader will buffer, `limit` bytes.
	MessageTooLarge(limit Int)
	// A write was given a binary that is not whole bytes.
	UnalignedBinary
	// A port outside 0..=65535.
	InvalidPort
	Errno(code Int)
}
